<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ScoutCode.ViewModels"
             x:Class="ScoutCode.Views.CipherDetailPage"
             x:DataType="vm:CipherDetailViewModel"
             Title="{Binding CipherName}"
             BackgroundColor="{StaticResource ScoutBg}">

    <Grid RowDefinitions="Auto,*">

        <!-- Tab Selector -->
        <Grid Grid.Row="0"
              ColumnDefinitions="*,*"
              Padding="20,14,20,6"
              ColumnSpacing="10">
            <Button Text="Manual"
                    Grid.Column="0"
                    Clicked="OnManualTabClicked"
                    x:Name="ManualTabBtn"
                    BackgroundColor="{StaticResource AccentIndigo}"
                    TextColor="White"
                    CornerRadius="22"
                    FontFamily="OpenSansSemibold"
                    FontAttributes="Bold"
                    HeightRequest="44" />
            <Button Text="Cámara"
                    Grid.Column="1"
                    Clicked="OnCameraTabClicked"
                    x:Name="CameraTabBtn"
                    BackgroundColor="{StaticResource SurfaceLight}"
                    TextColor="{StaticResource Gray500}"
                    CornerRadius="22"
                    FontFamily="OpenSansSemibold"
                    HeightRequest="44" />
        </Grid>

        <!-- Contenido con tabs -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="16" Spacing="12">

                <!-- ============================== -->
                <!-- SECCIÓN MANUAL -->
                <!-- ============================== -->
                <VerticalStackLayout IsVisible="{Binding IsManualMode}" Spacing="14">

                    <!-- Info de caracteres soportados -->
                    <Border BackgroundColor="{StaticResource Secondary}"
                            Stroke="Transparent"
                            StrokeShape="RoundRectangle 10"
                            Padding="14">
                        <Label FontSize="13"
                               TextColor="{StaticResource Gray500}">
                            <Label.FormattedText>
                                <FormattedString>
                                    <Span Text="Caracteres soportados: " FontAttributes="Bold" />
                                    <Span Text="{Binding SupportedCharsInfo}" />
                                </FormattedString>
                            </Label.FormattedText>
                        </Label>
                    </Border>

                    <!-- Selector de operación -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button x:Name="EncryptBtn"
                                Text="Cifrar"
                                Grid.Column="0"
                                Clicked="OnEncryptSelected"
                                BackgroundColor="{StaticResource AccentIndigo}"
                                TextColor="White"
                                CornerRadius="12"
                                FontFamily="OpenSansSemibold"
                                FontAttributes="Bold" />
                        <Button x:Name="DecryptBtn"
                                Text="Descifrar"
                                Grid.Column="1"
                                Clicked="OnDecryptSelected"
                                BackgroundColor="{StaticResource SurfaceLight}"
                                TextColor="{StaticResource Gray500}"
                                CornerRadius="12"
                                FontFamily="OpenSansSemibold" />
                    </Grid>

                    <!-- Entrada de texto -->
                    <Label Text="ENTRADA"
                           FontSize="12"
                           FontFamily="OpenSansSemibold"
                           TextTransform="Uppercase"
                           CharacterSpacing="1"
                           TextColor="{StaticResource Gray400}"
                           Margin="2,4,0,0" />

                    <Border Stroke="{StaticResource SurfaceMuted}"
                            StrokeShape="RoundRectangle 12"
                            StrokeThickness="1"
                            Padding="8"
                            MinimumHeightRequest="120"
                            BackgroundColor="White">
                        <Editor Text="{Binding InputText}"
                                Placeholder="Escribe aquí el texto..."
                                AutoSize="TextChanges"
                                MinimumHeightRequest="110"
                                FontSize="16"
                                FontFamily="OpenSansRegular" />
                    </Border>

                    <!-- Botones de acción -->
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                        <Button Text="{Binding OperationLabel, StringFormat='Procesar: {0}'}"
                                Command="{Binding ProcessCommand}"
                                Grid.Column="0"
                                BackgroundColor="{StaticResource AccentTeal}"
                                TextColor="White"
                                FontFamily="OpenSansSemibold"
                                FontAttributes="Bold"
                                CornerRadius="12"
                                HeightRequest="48" />
                        <Button Text="✕"
                                Command="{Binding ClearAllCommand}"
                                Grid.Column="1"
                                BackgroundColor="{StaticResource SurfaceLight}"
                                TextColor="{StaticResource Gray500}"
                                FontFamily="OpenSansSemibold"
                                CornerRadius="12"
                                WidthRequest="50"
                                HeightRequest="48" />
                    </Grid>

                    <!-- Mensaje de estado -->
                    <Label Text="{Binding StatusMessage}"
                           IsVisible="{Binding StatusMessage, Converter={StaticResource StringNotEmptyConverter}}"
                           FontSize="13"
                           FontFamily="OpenSansRegular"
                           TextColor="{StaticResource Gray400}"
                           HorizontalTextAlignment="Center"
                           Margin="0,4" />

                    <!-- Resultado -->
                    <VerticalStackLayout IsVisible="{Binding HasOutput}" Spacing="10">
                        <Label Text="RESULTADO"
                               FontSize="12"
                               FontFamily="OpenSansSemibold"
                               TextTransform="Uppercase"
                               CharacterSpacing="1"
                               TextColor="{StaticResource Gray400}"
                               Margin="2,4,0,0" />

                        <Border Stroke="{StaticResource SuccessBorder}"
                                StrokeShape="RoundRectangle 12"
                                StrokeThickness="1"
                                Padding="14"
                                MinimumHeightRequest="80"
                                BackgroundColor="{StaticResource SuccessBg}">
                            <Label Text="{Binding OutputText}"
                                   FontSize="16"
                                   FontFamily="OpenSansRegular"
                                   TextColor="{StaticResource SuccessText}" />
                        </Border>

                        <Button Text="Copiar resultado"
                                Command="{Binding CopyResultCommand}"
                                BackgroundColor="{StaticResource AccentIndigo}"
                                TextColor="White"
                                FontFamily="OpenSansSemibold"
                                CornerRadius="12"
                                HeightRequest="44" />
                    </VerticalStackLayout>
                </VerticalStackLayout>

                <!-- ============================== -->
                <!-- SECCIÓN CÁMARA (Placeholder) -->
                <!-- ============================== -->
                <VerticalStackLayout IsVisible="{Binding IsCameraMode}" Spacing="14">

                    <!-- Banner de desarrollo -->
                    <Border BackgroundColor="#FDF6EC"
                            Stroke="#E8D5B0"
                            StrokeShape="RoundRectangle 12"
                            StrokeThickness="1"
                            Padding="14">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="Módulo en Desarrollo"
                                   FontSize="14"
                                   FontFamily="OpenSansSemibold"
                                   FontAttributes="Bold"
                                   TextColor="#8B6914" />
                            <Label Text="El reconocimiento por cámara se implementará con OpenCV + ONNX Runtime."
                                   FontSize="13"
                                   FontFamily="OpenSansRegular"
                                   TextColor="{StaticResource Gray500}" />
                        </VerticalStackLayout>
                    </Border>

                    <!-- Área de preview -->
                    <Border Stroke="{StaticResource SurfaceMuted}"
                            StrokeShape="RoundRectangle 14"
                            StrokeThickness="1.5"
                            StrokeDashArray="8,4"
                            HeightRequest="200"
                            BackgroundColor="{StaticResource SurfaceLight}">
                        <VerticalStackLayout VerticalOptions="Center"
                                             HorizontalOptions="Center"
                                             Spacing="8">
                            <Label Text="CAM"
                                   FontSize="26"
                                   FontFamily="OpenSansSemibold"
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Gray300}"
                                   HorizontalTextAlignment="Center" />
                            <Label Text="Vista previa de imagen"
                                   FontSize="13"
                                   FontFamily="OpenSansRegular"
                                   TextColor="{StaticResource Gray400}"
                                   HorizontalTextAlignment="Center" />
                        </VerticalStackLayout>
                    </Border>

                    <!-- Botones de captura -->
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Text="Tomar foto"
                                Grid.Column="0"
                                Command="{Binding TakePhotoCommand}"
                                BackgroundColor="{StaticResource AccentIndigo}"
                                TextColor="White"
                                FontFamily="OpenSansSemibold"
                                CornerRadius="12"
                                HeightRequest="48" />
                        <Button Text="Elegir foto"
                                Grid.Column="1"
                                Command="{Binding PickPhotoCommand}"
                                BackgroundColor="{StaticResource Secondary}"
                                TextColor="{StaticResource AccentIndigo}"
                                FontFamily="OpenSansSemibold"
                                CornerRadius="12"
                                HeightRequest="48" />
                    </Grid>

                    <!-- Botón procesar cámara -->
                    <Button Text="Procesar imagen"
                            Command="{Binding ProcessCameraCommand}"
                            BackgroundColor="{StaticResource AccentTeal}"
                            TextColor="White"
                            FontFamily="OpenSansSemibold"
                            FontAttributes="Bold"
                            CornerRadius="12"
                            HeightRequest="48" />

                    <!-- Indicador de carga -->
                    <ActivityIndicator IsRunning="{Binding IsProcessing}"
                                       IsVisible="{Binding IsProcessing}"
                                       Color="{StaticResource AccentTeal}"
                                       HorizontalOptions="Center" />

                    <!-- Resultado de cámara -->
                    <Border IsVisible="{Binding HasCameraResult}"
                            Stroke="{StaticResource SurfaceMuted}"
                            StrokeShape="RoundRectangle 12"
                            Padding="14"
                            BackgroundColor="{StaticResource SurfaceLight}">
                        <Label Text="{Binding CameraResultText}"
                               FontSize="14"
                               FontFamily="OpenSansRegular"
                               TextColor="{StaticResource Gray600}" />
                    </Border>

                </VerticalStackLayout>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
